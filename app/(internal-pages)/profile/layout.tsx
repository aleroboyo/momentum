export const metadata = {
    title: "Momentum | Account Settings"
}

const Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default Layout