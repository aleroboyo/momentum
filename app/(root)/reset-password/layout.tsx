export const metadata = {
    title: "Momentum | Reset Password"
}

const Layout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default Layout
